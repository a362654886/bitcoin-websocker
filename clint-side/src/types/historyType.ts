export type chartData = {
  type: string;
  time: string[];
  data: number[];
};

export type historyData = {
  time: string;
  open: string;
  high: string;
  low: string;
  close: string;
  volume: string;
};
